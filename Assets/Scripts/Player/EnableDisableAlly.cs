using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnableDisableAlly : MonoBehaviour
{
    /*public float timer = 0f;
    public bool isActive = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        timer += Time.deltaTime;
        if (timer >= 10f && isActive)
        {
            disableAlly();
        }
    }
    public void enableAlly()
    {
        Debug.Log("Ally Enabled");
        FindGameObjectWithTag("Ally").SetActive(true);
        isActive = true;
        timer = 0;
    }
    public void disableAlly()
    {
        Debug.Log("Ally Disabled");
        this.SetActive(false);
        isActive = false;
    }*/
}
